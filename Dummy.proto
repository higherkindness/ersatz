syntax = "proto3";

package proto;

message User {
    uint64 ID = 1;
}

message Tweet {
    uint64 ID = 1;
    string text = 2;
    User user = 3;
}

message Timeline {
    repeated Tweet tweets = 1;
}